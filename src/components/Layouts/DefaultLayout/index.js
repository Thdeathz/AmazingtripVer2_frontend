
function DefaultLayout({ children }) {
    return (
        <div style={{ display: 'relative' }}>
            <h1>Layout</h1>
            {children}
        </div>
    );
}

export default DefaultLayout;